name: Test Basic Runner
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Basic echo
        run: echo "Runner works!"
      - name: Check date
        run: date
